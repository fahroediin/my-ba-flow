# Normalize line endings: store text as LF in the repo, check out as LF.
* text=auto eol=lf

# Treat these as binary (never normalize EOL).
*.plugin binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
